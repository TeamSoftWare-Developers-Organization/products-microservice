'use client';
import { useDispatch } from 'react-redux';import { Moon,Settings,Sun,Languages } from 'lucide-react';import { setLanguage,setTheme } from '@/store/slices/uiSlice';import { useTranslation } from '@/lib/translations';
export default function SettingsPage(){const dispatch=useDispatch();const {theme,language}=useTranslation();return <main className="admin-theme max-w-4xl mx-auto px-4 py-8"><h1 className="text-2xl font-black flex items-center gap-2 mb-6"><Settings className="text-emerald-500"/>إعدادات النظام</h1><div className="grid gap-4"><section className="surface-card p-5"><h2 className="font-black text-lg mb-3">المظهر</h2><div className="grid sm:grid-cols-2 gap-3"><button onClick={()=>dispatch(setTheme('light'))} className={`p-4 rounded-xl border flex gap-3 items-center ${theme==='light'?'border-emerald-500 bg-emerald-500/10':'border-border'}`}><Sun className="text-amber-500"/>الوضع الفاتح</button><button onClick={()=>dispatch(setTheme('dark'))} className={`p-4 rounded-xl border flex gap-3 items-center ${theme==='dark'?'border-emerald-500 bg-emerald-500/10':'border-border'}`}><Moon className="text-indigo-400"/>الوضع الداكن</button></div></section><section className="surface-card p-5"><h2 className="font-black text-lg mb-3 flex gap-2 items-center"><Languages className="w-5 h-5"/>اللغة</h2><div className="grid sm:grid-cols-2 gap-3"><button onClick={()=>dispatch(setLanguage('ar'))} className={`p-4 rounded-xl border ${language==='ar'?'border-emerald-500 bg-emerald-500/10':'border-border'}`}>العربية</button><button onClick={()=>dispatch(setLanguage('en'))} className={`p-4 rounded-xl border ${language==='en'?'border-emerald-500 bg-emerald-500/10':'border-border'}`}>English</button></div></section><section className="surface-card p-5"><h2 className="font-black text-lg">معلومة التشغيل</h2><p className="text-sm text-muted-foreground mt-2">يتم حفظ المظهر واللغة محليًا وتطبيقهما على جميع واجهات النظام عبر AppShell العام.</p></section></div></main>}
